# =============================================================
# DON ALEX — Variables de entorno raíz (ejemplo)
# Copiar como .env y completar con valores reales.
# NUNCA commitear el archivo .env con valores reales.
# =============================================================

# ── PostgreSQL ────────────────────────────────────────────────
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# ── Puertos expuestos (opcional, defaults en docker-compose) ──
DB_PORT=
BACKEND_PORT=
FRONTEND_PORT=
