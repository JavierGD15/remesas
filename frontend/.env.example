# =============================================================
# DON ALEX — Frontend variables de entorno (ejemplo)
# Copiar como .env y completar con valores reales.
# NUNCA commitear el archivo .env con valores reales.
# Solo las variables prefijadas con VITE_ son expuestas al cliente.
# =============================================================

# ── API ───────────────────────────────────────────────────────
VITE_API_BASE_URL=
VITE_API_TIMEOUT=

# ── App ───────────────────────────────────────────────────────
VITE_APP_NAME=
VITE_APP_ENV=
